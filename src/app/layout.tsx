import type { Metadata } from 'next'
import { Inter, Space_Grotesk } from 'next/font/google'
import './globals.css'

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
})

const spaceGrotesk = Space_Grotesk({
  subsets: ['latin'],
  variable: '--font-space-grotesk',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Olswen Independence Alliance — Security, Intelligence & Risk Management',
  description: "Nigeria's premier intelligence-driven security consultancy. Trusted by NFL Africa, Davido, Kai Cenat, IShowSpeed, FIFA and more.",
  keywords: 'security company Nigeria, close protection Lagos, event security Nigeria, intelligence risk management, VIP protection Lagos',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${spaceGrotesk.variable}`}>
        {children}
      </body>
    </html>
  )
}
